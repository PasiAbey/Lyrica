services:

  frontend:
    image: pasiabey/lyrica-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
  
  backend:
    image: pasiabey/lyrica-backend:latest
    ports:
      - "3000:3000"
    volumes:
      - ./backend/lyrics_db.json:/app/lyrics_db.json